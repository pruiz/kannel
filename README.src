Directory organization of the gateway source tree


The source tree is organized as follows:

	gateway		root of source tree: some docs, no sources
	|-- contrib	contributed extra material, not really used (yet)
	|-- doc		documentation
	|-- gw		the gateway itself
	|-- gwlib	utility functions for gateway and other programs
	|-- test	programs for testing the gateway
	`-- utils	utility programs

